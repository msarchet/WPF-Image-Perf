<UserControl
    x:Class="ImagePerf.LabelInput"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImagePerf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="300">

    <RelativePanel Height="50">
        <TextBlock x:Name="lbl" FontSize="16" Text="{x:Bind Label}" RelativePanel.AlignVerticalCenterWithPanel="True"></TextBlock>
        <TextBox x:Name="val" RelativePanel.RightOf="lbl" FontSize="16" Text="{x:Bind Value, Mode=TwoWay}" RelativePanel.AlignVerticalCenterWithPanel="True" TextAlignment="Center" Padding="10, 0, 0, 0" RelativePanel.AlignRightWithPanel="True"></TextBox>
    </RelativePanel>
</UserControl>
